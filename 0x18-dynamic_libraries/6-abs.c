#include "main.h"
/**
 *_abs - computes the absolute value of an integer.
 *
 *@n: n is char to be computed.
 *Return: abs() value.
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
_putchar('\n');
}
